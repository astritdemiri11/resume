import { AttrEffectDirective } from './attr-effect.directive';

describe('AttrEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new AttrEffectDirective();
    expect(directive).toBeTruthy();
  });
});
